# Data Directory

This directory contains:
- `meeting_database.json`: Local database mapping meeting URLs to S3 file paths

The database file is automatically created when the first meeting is processed.
